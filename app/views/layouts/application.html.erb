<!DOCTYPE html>
<html>
  <head>
    <title>BrewDo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

<%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= yield :head if content_for?(:head)%>
  </head>

  <body>
    <%= render partial: 'layouts/nav' unless current_page?(root_path)%>
    <div class="wrapper">
      <%= yield %>
    </div>
    <%= render partial: 'layouts/footer' unless current_page?(root_path)%>
  </body>
</html>
